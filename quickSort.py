#QuickSort é um método de ordenação baseado numa abordagem de divisão-e-conquista, com recursão
#Inicialmente: p = 0; q = len(v)-1

#def quicksort(v,p,q):
#    if p<q:
#        pivot = partir(v,p,q)
#        quicksort(v,p,pivot-1)
#        quicksort(v,pivot+1,q)